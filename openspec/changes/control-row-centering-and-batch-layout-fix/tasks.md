## 1. CSS

- [x] 1.1 在 `.control-row` 加入 `justify-content: center`
- [x] 1.2 新增 `.btn-group` class：`display: flex; align-items: center; gap: 8px; flex-shrink: 0`

## 2. HTML — control-row 按鈕分組

- [x] 2.1 用 `<div class="btn-group">` 包裹 `btn-hide-completed` 和 `btn-overtime-filter`
- [x] 2.2 用 `<div class="btn-group">` 包裹 `btn-copy-remaining`（含 copy-menu-popup）和 `btn-search-toggle`

## 3. HTML — compact strip 短標籤

- [x] 3.1 將 `btn-hide-completed-compact` 的 label-long/label-short span 移除，改為固定文字「隱藏」
- [x] 3.2 將 `btn-overtime-filter-compact` 的 label-long/label-short span 移除，改為固定文字「加時」

## 4. HTML — selection action bar 短標籤

- [x] 4.1 將 `btn-select-all-incomplete` 的 label-long/label-short span 移除，改為固定文字「全選」
- [x] 4.2 將 `btn-select-clear` 的 label-long/label-short span 移除，改為固定文字「清除」
- [x] 4.3 將 `btn-selection-confirm` 的 label-long/label-short span 移除，改為固定文字「確認」
