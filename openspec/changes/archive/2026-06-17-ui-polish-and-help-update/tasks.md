## 1. CSS 縮減 Header 與 Footer

- [x] 1.1 修改 `.app-header` padding：`18px 20px` → `10px 20px`
- [x] 1.2 修改 `.pokeball-icon` font-size：`1.4rem` → `1.2rem`
- [x] 1.3 修改 `.app-header h1` font-size：`1.25rem` → `1.1rem`
- [x] 1.4 修改 `.btn` 全域 padding：`14px 24px` → `10px 20px`
- [x] 1.5 移除 `@media (max-width: 480px)` 中的 `.app-header` padding 覆寫規則（已由全域統一，不再需要）

## 2. 說明頁面更新（index.html）

- [x] 2.1 更新「五、搜尋桌號」章節：
  - 「搜尋欄位」說明改為：點擊按鈕列最右側 🔍 按鈕展開搜尋欄位，輸入桌號後自動捲動至該桌並高亮顯示；再次點擊 🔍 或清空欄位可關閉搜尋
  - 「找不到時」維持不變
  - Tip 補充：「批次選取模式下 🔍 自動隱藏」
- [x] 2.2 更新「七、批量設為已完成」章節：
  - 「進入選取模式」說明補充：進入後上方資訊列切換顯示輪次進度與已選桌數
  - 新增說明項目「篩選器可用」：批次模式中可點擊資訊列右側的「隱藏完成」與「只顯示加時」篩選器縮小目標範圍

## 3. PWA 快取更新

- [x] 3.1 更新 `sw.js` 快取版本號（`v15` → `v16`）
