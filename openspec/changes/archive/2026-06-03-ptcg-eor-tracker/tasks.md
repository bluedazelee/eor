## 1. 專案結構與靜態資源初始化

- [x] 1.1 建立首頁網頁 `index.html`，規劃開始畫面與紀錄畫面的基本 DOM 容器。
- [x] 1.2 建立樣式表 `style.css`，配置亮/暗色調色盤與 CSS 變數，設定響應式 Grid 佈局以及主畫面的卡片樣式。
- [x] 1.3 建立應用程式邏輯檔 `app.js`，宣告全域狀態結構，並實作最基本的視圖切換邏輯。
- [x] 1.4 新增 PWA 描述檔 `manifest.json` 與相關尺寸的 App 圖示（如 192x192, 512x512 等）。

## 2. 核心狀態與互動邏輯實作 (eor-tracker-core)

- [x] 2.1 實作開始畫面的輸入表單驗證（起始桌號與結束桌號必須為正整數且起始小於等於結束），並點擊開始後初始化各桌次狀態，將資料序列化存入 LocalStorage。
- [x] 2.2 實作 LocalStorage 還原機制，使網頁載入時若偵測到未完成的輪次資料，能直接回復至紀錄主畫面。
- [x] 2.3 實作卡片列表的渲染邏輯與狀態點擊循環切換 (尚未結束 -> 已分配 -> 已完成 -> 尚未結束)，並在切換至「已分配」時，寫入當前系統時間 HH:mm 並在卡片中呈現。
- [x] 2.4 實作統計數據更新邏輯（已完成桌數、剩餘桌數、總桌數與百分比進度條），每次點擊卡片時重新計算並渲染。
- [x] 2.5 實作「顯示已完成桌號」Checkbox 的勾選控制邏輯，控制是否隱藏或顯示「已完成」狀態的桌號圖卡。
- [x] 2.6 實作「確認本輪處理完畢」按鈕，當所有卡片皆為已完成狀態時啟用。點擊後跳出確認視窗，確認後清除 LocalStorage 並重設 SPA 回到開始畫面。

## 3. PWA 與 Service Worker 離線支援 (eor-tracker-pwa)

- [x] 3.1 實作 Service Worker 檔案 `sw.js`，實作快取靜態資源的安裝、激活與 Fetch 攔截事件 (採用 Cache-First 策略)。
- [x] 3.2 於 `app.js` 中註冊 Service Worker。
- [x] 3.3 驗證 PWA 離線工作狀態：在無網路環境下（或 Chrome 開發者工具開啟 Offline 模式）確保網頁能完全正常載入且執行。
