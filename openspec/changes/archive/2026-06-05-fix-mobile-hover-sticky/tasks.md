## 1. CSS 修改

- [x] 1.1 將 `.btn-filter-complete:hover` 包入 `@media (hover: hover)` 媒體查詢
- [x] 1.2 將 `.btn-filter-copy:hover` 包入同一 `@media (hover: hover)` 區塊
- [x] 1.3 將 `.btn-filter:hover`（只顯示加時桌按鈕）包入 `@media (hover: hover)` 媒體查詢
