## 1. style.css — 修正 hover 殘留

- [x] 1.1 將 `.btn-primary:hover`（約第 276 行）包入 `@media (hover: hover) { ... }`
- [x] 1.2 將 `.btn-success:hover:not(:disabled)`（約第 290 行）包入 `@media (hover: hover) { ... }`
- [x] 1.3 將 `.btn-warning:hover`（約第 1005 行）包入 `@media (hover: hover) { ... }`
- [x] 1.4 將 `.btn-sel-cancel:hover`（約第 1069 行）包入 `@media (hover: hover) { ... }`
- [x] 1.5 將 `.btn-sel-shortcut:hover`（約第 1107 行）包入 `@media (hover: hover) { ... }`
