## 1. CSS 樣式

- [x] 1.1 修改 `.left-controls`：`flex-direction: column` → `flex-direction: row`，並加入 `align-items: center`
- [x] 1.2 修改 `.undo-redo-btns button`：padding 從 `4px 8px` → `5px 8px`

## 2. PWA 快取更新

- [x] 2.1 更新 `sw.js` 快取版本號（`v14` → `v15`）
