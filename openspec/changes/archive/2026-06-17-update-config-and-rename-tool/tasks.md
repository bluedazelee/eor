## 1. 工具名稱更新

- [x] 1.1 將 `config.yaml` 第 5 行的「PTCG EoR Helper」改為「PTCG EoR Tracker」

## 2. 狀態管理更新

- [x] 2.1 將 `config.yaml` 的 `狀態管理` 區段中 `roundName: string` 改為 `roundNumber: number`，並更新說明文字反映數字輪次（1–20，顯示格式 R1、R2…）

## 3. 組別說明更新

- [x] 3.1 將 `config.yaml` 的 `組別選項` 區段改為「組別為自由輸入文字欄位（必填），上次輸入的內容會被記住」，移除固定清單

## 4. 主要功能清單更新

- [x] 4.1 將「全部設為已完成（含確認對話框）」改為「批量設為已完成（格線選取模式：點選桌次 / 全選未完成 / 清除全選 / 反選）」
- [x] 4.2 將「複製剩餘桌次資訊到剪貼簿（格式：組別 → 剩餘加時桌 → 剩餘一般桌）」改為「複製資訊到剪貼簿（本輪範圍 / 加時桌次 / 剩餘桌次 / 詳細資訊）」
- [x] 4.3 新增「桌號搜尋（輸入桌號自動捲動至該桌並高亮）」
- [x] 4.4 新增「復原 / 重做（最多 20 步，批量操作亦可一步還原）」
